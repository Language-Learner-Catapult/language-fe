import React from 'react';
import { BottomNavigation } from '@mui/material';

const Footer = () => {
    return (
        <BottomNavigation showLabels style={{ width: '100%', position: 'absolute', bottom: 0 }}>
            {/* Footer content goes here, maybe some navigation links or copyright text */}
        </BottomNavigation>
    );
};

export default Footer;
